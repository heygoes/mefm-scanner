name: MEFM Scanner

on:
  schedule:
    - cron: '0 11 * * 1-5'
    - cron: '0 22 * * 1-5'
  workflow_dispatch:

jobs:
  run-scanner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install libraries
        run: pip install -r requirements.txt
      - name: Run scanner
        run: python mefm_scanner_v6c_fast.py
        env:
          LINE_CHANNEL_ACCESS_TOKEN: ${{ secrets.LINE_CHANNEL_ACCESS_TOKEN }}
          LINE_USER_ID: ${{ secrets.LINE_USER_ID }}
```

---

## ② mefm_scanner_v6c_fast.pyをGitHubに上書きアップロード

今日Colabで動かした最新版（ダウンロードしたファイル）を、GitHubに**上書きアップロード**します。
```
GitHubのリポジトリ画面
↓
mefm_scanner_v6c_fast.py をクリック
↓
右上の鉛筆マーク（Edit）
↓
全選択して削除
↓
新しいコードを貼り付け
↓
Commit changes
```

---

## ③ LINEのSecretsを登録（まだなら）
```
GitHub → Settings
→ Secrets and variables
→ Actions
→ New repository secret

LINE_CHANNEL_ACCESS_TOKEN → トークンを貼り付け
LINE_USER_ID              → USER_IDを貼り付け
```

---

## 完了後のテスト
```
GitHub → Actions
→ MEFM Scanner
→ Run workflow（手動実行）
→ LINEに通知が届けば完成
